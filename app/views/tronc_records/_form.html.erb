<%= form_with(model: tronc_record, local: true) do |form| %>
  <% if tronc_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tronc_record.errors.count, "error") %> prohibited this tronc_record from being saved:</h2>

      <ul>
        <% tronc_record.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :gross_tips %>
    <%= form.number_field :gross_tips %>
  </div>

  <div class="field">
    <%= form.label :week_end %>
    <%= form.date_select :week_end %>
  </div>

  <div class="field">
    <%= form.label :tax_due %>
    <%= form.number_field :tax_due %>
  </div>

  <div class="field">
    <%= form.label :employee_record_id %>
    <%= form.text_field :employee_record_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
